# This file is managed by salt, at {{ source }}

# Configuration for my personal website. It's expected that it'll be
# self-contained and static. See also,
# https://github.com/evaryont/evaryont.github.io

etag on;

root {{ pillar['personal_website_dir'] }};

location / {
  index index.html;
  autoindex off;
  expires 21d;
}

location /assets {
  expires max;
}
